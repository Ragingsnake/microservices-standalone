# frontend

Canary demo images (`v2-good`, `v2-bad`): see [CANARY-BUILD-PLAN.md](./CANARY-BUILD-PLAN.md).

Run the following command to restore dependencies to `vendor/` directory:

    dep ensure --vendor-only
